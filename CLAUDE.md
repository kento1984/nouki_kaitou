# 納期回答書ツール プロジェクト

## ファイル構成

- `nouki_kaitou/` パッケージ（Python実装）
  - `main.py` - エントリーポイント（CLI + GUI起動）
  - `gui.py` - tkinter GUI（顧客選択・期間指定）
  - `data_loader.py` - SAPデータ読込（.xls）
  - `delivery_calc.py` - 納期計算コア
  - `report_generator.py` - レポート生成・分類処理
  - `email_builder.py` - メール本文生成
  - `bunno.py` - 分納処理
  - `confirming.py` - 確認中一覧参照
  - `history.py` - 送付履歴管理
  - `customer.py` - 顧客マスター参照
  - `manufacturer.py` - メーカーマスター参照
  - `representative.py` - 担当者マスター参照
  - `tracking.py` - 送り状情報抽出
  - `stockout.py` - 欠品情報抽出
  - `business_days.py` - 営業日計算
  - `excel_writer.py` - Excel出力
  - `models.py` - データモデル定義
  - `utils.py` - ユーティリティ関数

## 業務背景

マツモト産業の納期回答書ツール。SAPの受注データから納期を計算し、お客様向けの納期回答書PDF + メールを自動生成する。

### 伝票タイプ

- **在庫販売**：自社在庫からの販売
- **直送**（直送販売 + 保管場所=転送中（直送用））：メーカーからお客様へ直送
- **紐付き**（直送販売 + 保管場所≠転送中（直送用））：一旦自社倉庫を経由してからお客様へ配送

### 出荷ステータス（SAPの3種類）

- **未処理**：まだ出荷していない
- **一部処理済み**：分納の一部だけ出荷済み
- **処理完了**：全数出荷済み

## 基本運用

- 納期回答は1日2回実行
- 12/31は「未確定」を示す特殊値（指定納期・受注納期どちらも）

## 納期計算ルール（重要）

- `calculate_delivery_date`関数（delivery_calc.py）の冒頭コメントに納期表示ルール表あり
- 納期計算に関わる修正をする際は、**必ずこのコメントを読んでから作業すること**
- 「配達予定」は自社便ルート配達の場合のみ。路線便は「出荷予定」

### 指定納期の意味（伝票タイプで異なる）

- 在庫販売：指定納期＝客の希望到着日
- 直送販売・紐付き販売：指定納期＝メーカー出荷日
- 引取・作業(&&)・着日指定(@@)は特殊パターンで先に処理される

### 指定納期の優先ルール

- 指定納期が12/31以外で設定されている場合、全伝票タイプ（在庫販売・直送・紐付き）共通で、処理完了かどうかに関係なく指定納期パスが先に評価される
- 在庫販売の用途: センターに無理を言って通常より早く配達する場合や、先の日程で納入指示がある場合
- 処理完了+指定納期ありでも、15時カットオフではなく指定納期の日付で計算される
- 在庫販売は指定納期パス内で転送中/出荷ルール/配達の3分岐
- 直送（転送中）は指定納期をそのまま出荷予定として使用
- 紐付き（非転送中）は指定納期に配送加算日数・曜日制限・路線便ルールを適用

### 15時カットオフルール（在庫販売・紐付き共通）

- 15:00以前に処理完了 → 翌営業日配達予定
- 15:01以降に処理完了 → 翌々営業日配達予定
- 受注先≠出荷先の場合は出荷予定（1営業日前）
- **注意**: 現在、在庫販売は`_before_cutoff`（15:00ちょうど=締切前）、紐付きは`exec_hour < cutoff`（15:00ちょうど=締切後）で判定が異なる。TODO: 統一すべき

### 配達/出荷の使い分け

- 受注先=出荷先 → 配達予定/配達済み
- 受注先≠出荷先 → 出荷予定/出荷済み（1営業日前を出荷日として計算）
- 他拠点からの出荷 → 出荷予定/出荷済み

### 在庫販売で路線便・受注先≠出荷先の場合

- 出荷から到着まで日数がかかるため、指定納期そのまま出荷日にしてはいけない
- 指定納期（到着希望日）から逆算して出荷日を決める必要がある

### 紐付き処理完了で路線便・受注先≠出荷先の場合

- 配達日（マクロ実行時刻から算出）から1営業日逆算して出荷予定
- 在庫販売の逆算（指定納期から）とは別のロジック

### @@着日指定の処理

- コメント（社内）に`@@`または`＠＠`があれば着日指定として処理
- `@@`の直後からM/D形式の日付を抽出
- 出荷日は指定納期→受注納期の優先順位で取得（12/31はスキップ）
- 両方12/31なら処理スキップ（日程調整中に落ちる）
- 出力: 「○月○日出荷済→○月○日着」または「○月○日出荷→○月○日着予定」

### コメント特殊マーカー

| マーカー | 意味 |
|----------|------|
| `&&` | 作業案件フラグ。日付は`&&`の後ではなく受注納期に入れる |
| `@@`（`＠＠`） | 着日指定フラグ。直後にM/D形式で着日を記載 |
| `##`（`＃＃`） | 除外マーカー（回答書から除外） |
| `$$`（`＄＄`） | 価格確認中フラグ。$は2つ必要（1つでは無視）。`$$`があれば確認中でも単価・金額をそのまま表示 |
| キャンセル | コメントに含まれる場合はSAPで処理完了にすべき案件 |

## 業務ルール（確定済み）

### 基本動作

- 確定伝票 → 送付履歴に記録 → 次回以降スキップ
- 未確定伝票 → 確認中一覧に記録 → 正式回答するまで毎回出力

### 送付履歴の管理

- 送付履歴にあり + `registration_date < today` → 回答書に出力しない
- `registration_date == today` → 何度でも回答書に記載される（1日2回の運用に対応）
- 処理完了 + 送付履歴ステータス「確認中」→ 今回出力する（確認中が確定したため）

### 処理完了時の動作

| ケース | 動作 |
|--------|------|
| 直送 + 処理完了 | 「納品済み」で送付履歴へ |
| 紐付き + 処理完了 | `calculate_delivery_date`で計算された納期で回答 → 送付履歴へ |

※ 直送で処理完了時に残っている伝票は、メーカーから納期回答が来ないまま出荷・伝票計上されたケース（指定納期が12/31のまま）。指定納期に日付が入っていれば、処理完了より前に送付履歴に入っているため、このタイミングで残っていることはない。

紐付きの処理完了時の納期計算：
- 受注先＝出荷先（自社便）：15時前なら「翌営業日配達予定」
- 受注先≠出荷先：15時前なら「本日出荷予定」（配達日から1営業日逆算）
- 路線便：配達日から1営業日逆算して出荷予定

### 欠品の扱い

- **特別扱い不要**
- 処理完了前：「欠品中」で確認中一覧に記録、毎回出力
- 処理完了後（直送）：「納品済み」→ 送付履歴へ
- 処理完了後（紐付き）：計算された納期で回答 → 送付履歴へ
- 欠品コメントの有無に関係なく、処理完了になれば通常の納期計算で回答

### 分納の扱い

- 分納は直送・紐付きのみ（在庫販売で分納はない）
- 未定が残っている → 確認中一覧に「分納」で残し続ける
- 全分確定（未定なし）→ 「分納完了」で送付履歴へ
- 出荷ステータスが「一部処理済み」の場合は未処理と同じ扱い（特別な処理なし）
- 分納+処理完了 → コメントに「未定」が残っていても全出荷済み（SAPの制約参照）。「納品済み」で回答し、ご連絡事項・メールに「分納の商品は全て出荷完了」と1回案内してから送付履歴に「分納完了」で記録

### SAPの制約（重要）

- 処理完了になると受注の変更ができなくなる（コメント欄も編集不可）
- そのためコメント欄の「未定」「欠品中」等の文言は処理完了後も残り続ける
- 処理完了時のロジックではコメント欄の内容を信用しないこと（処理完了＝全出荷済みが事実）
- この制約の補完として、確認中一覧に出荷日を手入力 → `get_confirmed_delivery_date`で取得 → `calculate_bunno_date`の納期計算に反映する仕組みがある

### 未確定判定

- 指定納期を優先、なければ受注納期を見る
- 両方12/31 → 未確定（在庫販売：日程調整中、直送：確認中）

### 担当者マスター

- 顧客マスターに登録されていない担当者の注番は`__OTHER__`としてまとめる
- `__OTHER__`の場合、メール件名に担当者名を含めない
- セルが空欄または未登録担当者が1人でもいれば`__OTHER__`に含まれる

### 送料行の運用

- 送料行は本体と一緒に処理される
- 本体が出荷済みで送料だけ残る場合は「明細削除」がSAPで設定される

## メール生成

- 送り状情報 → 欠品 → 分納 → 分納完了 の順にセクション表示
- 欠品中 → 専用セクション「欠品中の商品について」（欠品リストが空でなければ表示）
- 分納 → 専用セクション「分納のご連絡」、未定ありなら赤字注記（分納リストが空でなければ表示）
- 分納完了 → 専用セクション「分納完了のご連絡」（完了リストが空でなければ表示）
- 確認中注記 → フッターに常に追加（確認中の商品がなくても表示）

## 主要な関数・変数

### VBA → Python 対応表

| VBA関数 | Python関数 | ファイル |
|---------|------------|----------|
| CalculateDeliveryDate | `calculate_delivery_date` | delivery_calc.py |
| GetConfirmingStatus | `get_confirming_status` | confirming.py |
| GetConfirmedDeliveryDate | `get_confirmed_delivery_date` | confirming.py |
| HasBunnoMitei | `has_bunno_mitei` | bunno.py |
| ExtractBunnoInfo | `extract_bunno_info` | bunno.py |
| CalculateBunnoDate | `calculate_bunno_date` | bunno.py |
| BuildEmailBodyHTML | `build_email_body_html` | email_builder.py |
| CleanConfirmingList | `clean_confirming_list` | history.py |

### 主要な変数（report_generator.py内）

| 名前 | 役割 |
|------|------|
| `is_bunno_in_confirming` | 分納で確認中一覧に残っている場合True（`force_delivered`をブロック） |
| `keep_in_confirming` | 分納で確認中一覧に残すべき場合True（振り分けで使用） |
| `is_himozuki` | 紐付き伝票ならTrue（`force_delivered`は常にFalse） |
| `force_delivered` | 直送で処理完了時に「納品済み」にするフラグ |
| `is_bunno_completed` | 分納+処理完了で「分納完了」として処理するフラグ |

## 確認中一覧の色ルール

- 分納 → 「分納」（青色）
- 欠品 → 「欠品中」（紫色）
- それ以外 → SAP出荷ステータスそのまま（通常「未処理」）
- 色条件に一致しない行は `PatternFill(fill_type=None)` で明示的にクリアすること（ソート後に以前の色が残るバグの対策）

## 修正済みバグ（設計判断の背景）

### 全角半角正規化（utils.py, delivery_calc.py, report_generator.py）

- **問題**: SAPデータで顧客名に全角括弧`（有）`と半角括弧`(有)`が混在。`customer_name != ship_to_name` の比較が失敗し、「配達予定」が「出荷予定」になる
- **修正**:
  - utils.py: `normalize_name_for_comparison()` 関数追加
  - delivery_calc.py: `_is_same_customer()` ヘルパー追加、4箇所の比較を正規化対応
  - report_generator.py: 分納計算の1箇所を正規化対応
- **注意**: 顧客名比較を追加する場合は必ず正規化関数を使うこと

### 送付履歴書き込み（history.py）3件

| 問題 | 修正 |
|---|---|
| 明細列が文字列"10"で書き込まれる | `_to_int_detail()` で数値10に変換 |
| 送付者列が空 | `_get_default_sender()` でWindowsユーザー名を自動取得 |
| 確認中一覧の受注納期にSAPデータが入る | 常にNone（空白）で書き込む（ユーザー手入力欄のため） |

### 確認中一覧の色クリア（excel_writer.py）

- **問題**: 色条件に一致しない行の古い色が残る（ソート後に以前の黄色が新しい行に残る）
- **修正**: `PatternFill(fill_type=None)` で明示的に色クリア

### 15時カットオフ統一（delivery_calc.py）

- **問題**: 在庫販売は`_before_cutoff`（`hour < cutoff or (hour == cutoff and minute == 0)`）で15:00ちょうど＝締切前、紐付きは`exec_hour < cutoff`で15:00ちょうど＝締切後と判定が不一致
- **修正**: `_before_cutoff`を`hour < cutoff`に変更し、15:00ちょうど＝締切後（間に合わない）に統一

## 運用上の注意点

### 送付履歴の共有

- VBAとPythonは**同じ送付履歴.xlsx**に書き込む（並行運用期間中）
- 送付履歴が破損した場合: ファイルサーバーの「以前のバージョン」から復元可能（シャドウコピーは毎日取られている）

### 確認中一覧

- 受注納期列は常に空白で書き込む。ユーザーが手動で日付を入力 → 次回実行時に `get_confirmed_delivery_date` で取得 → 納期回答に反映

### 全角半角問題

- SAPデータに全角/半角混在あり（`（有）` vs `(有)`、`（株）` vs `(株)` 等）
- 6箇所の顧客名比較で正規化済み

### Cドライブ容量

- 会社PCのCドライブは容量が少ない（175GB中、空き1.4GB程度）
- 定期的にTemp掃除が必要

### データファイルの場所（会社ネットワーク）

- 受注一覧: `\\flsv04\316京葉\納期回答書ツールフォルダ\受注一覧\`
- メーカー一覧等: `\\flsv04\316京葉\納期回答書ツールフォルダ\`
- 回答書出力: `\\flsv04\316京葉\納期回答書ツールフォルダ\納期回答書\`
- 送付履歴: `\\flsv04\316京葉\納期回答書ツールフォルダ\送付履歴.xlsx`

## レビュー時の注意

- 欠品を `is_bunno_in_confirming` や `keep_in_confirming` に含めないこと（意図的な仕様）
- `isBunnoOrKeppin` は旧VBA変数名。Pythonコードに残っていたらバグ
- 在庫販売で分納は発生しない（運用上あり得ない）。コードにガードはないが実害なし
- 分納+処理完了は「分納完了」で1回案内後、送付履歴に記録して次回スキップ
- 紐付き+処理完了は1回回答後、送付履歴に記録して次回スキップ

## ビルド

- コード変更後は必ず `python -m PyInstaller nouki_kaitou.spec --clean --noconfirm` で再ビルドすること

## 既知の課題・TODO

- `has_bunno_mitei`の重複呼び出し整理（report_generator.py）
- 顧客マスター未登録6社の追加検討
- 180日ルール（年またぎ日付の翌年補正）の値が固定。設定可能にするか検討
- 分納判定のコメント（社内）対応 — 現在コメント（明細）のみ参照。コメント（社内）にも分納情報がある場合がある
- VBA完全廃止 — 十分な並行運用テスト後にVBAからPythonに完全移行
