# 引継ぎ資料 2025/02/18-27

## 賢人さんが何をやろうとしているか

賢人さんは自分で書いたVBAマクロ（7,758行）をPythonに移植している。納期回答書という、顧客への納品スケジュールを自動生成するツール。SAPの受注データを読み込み、納期を計算し、Excel帳票を作り、メールで送信するまでを自動化している。

移植の目的は単なる言語変更ではない。最終的にはWebアプリ化して、会社のPCに縛られず、出先や自宅からでもブラウザで納期回答業務ができるようにすること。Python化はそこへの布石。VBAではWeb化ができないため、Pythonに移植した。

現在の段階:
1. ✅ VBA → Python 1:1置き換え（完了）
2. ✅ PyInstaller exe化（完了。他営業所への配布可能に）
3. ✅ 他営業所展開準備（運用マニュアル作成中）
4. ✅ パフォーマンス最適化（2/22完了）
5. ✅ 仙台営業所対応（2/26設計・実装完了。テスト待ち）
6. ✅ 社外コメント表示機能（2/27実装完了。月曜に配布予定）
7. → VBA廃止（並行運用で信頼性を積み上げ中）
8. → Web化（FastAPI + React。賢人さんは両方勉強済み）

他営業所（松本・高崎・姫路）にも展開が始まっており、藤井課長（松本営業所）から運用に関する質問が来ている状況。仙台営業所（佐藤さん）の配送ルール対応も設計・実装完了。

## プロジェクトの規模

| 区分 | 行数 |
|--|--|
| VBAマクロ（移植元） | 7,758行 |
| Pythonプロダクションコード | 約9,500行 |
| Pythonテストコード | 約11,800行 |
| Python合計 | 約21,300行 |
| テスト件数 | 1,109件 |

プロダクションコードがVBAより多いのは、VBAではExcel自体がやっていた部分（GUI 722行、ファイル操作等）をPythonでは自前で書く必要があったため。テストはVBAでは0行だったものが11,255行以上になり、安全な改修の土台になっている。

## Claude.aiとClaude Codeの役割分担

**Claude.ai（このチャット）= 司令塔・相談相手**
- 方針の相談、メールの作成、業務判断の壁打ち
- Claude Codeへの指示文を一緒に考える
- Claude Codeの出力結果を一緒に読んで判断する
- Manusへの指示書もClaude.aiで作成

**Claude Code = 実行者**
- 賢人さんのPC上で動いている
- コードの修正、テスト実行、git操作、PyInstallerビルドを実際にやる
- 賢人さんがClaude.aiで作った指示文をClaude Codeに投げる

**Manus = スライド作成者（新規）**
- PowerPointの作成を担当
- GitHubリポジトリのコードを読んで業務ロジックを理解
- NANOバナナで仮画像を生成（実画像は後で差し替え）
- Claude.aiで作った指示書に基づいてスライドを作成

典型的な流れ:
1. 賢人さんがClaude.aiに相談 →「こういう指示でいい？」
2. Claude.aiが指示文を提案
3. 賢人さんがClaude Code or Manusにその指示を投げる
4. 出力結果を賢人さんがClaude.aiに貼る
5. 一緒に結果を確認して次のアクションを決める

賢人さんはClaude CodeにもManusにも丸投げしない。結果を必ず確認し、おかしいところは突っ込む。

## 2/18の作業内容

### 1. Python版の日次検証（1,521件）
- compare_realtime.py（AM/PM）+ compare_week.py（週次）で合計1,521件をVBAと比較
- Pythonバグ: 0件
- VBA側のバグ1件発見（15時カットオフの不統一）→ Python側は正しかった

### 2. 15時カットオフ修正
- delivery_calc.py: 在庫販売の処理完了判定を `hour < cutoff` に統一
- VBAでは15:00ちょうど=締切前、Pythonでは締切後に統一（紐付きと合わせた）

### 3. 業務ルール確認表の作成
- dev_scripts/generate_business_rules.py → dev_scripts/business_rules.xlsx
- 4シート: 納期計算ルール（42行）+ 凡例 + 用語集 + 具体例（74件）
- 藤井課長への説明資料として作成

### 4. 藤井課長メールへの回答作成
- 特殊記号（@@, $$, ##, &&）の動作説明
- GT0Z403474（イーグル点検）が「確認中」になる理由の解明
- 推奨: &&で「作業予定」表示
- 社外コメントは備考欄に未反映（ソースはcomment_detailのみ）
- 社内コメントの価格式は特殊記号として読まれないので安全

### 5. VBA↔Python コード突合せ

これが2/18の最重要作業。「1,521件データ比較でバグゼロ」「912テスト全通過」でも安心できないという賢人さんの判断で、VBAのコードとPythonのコードを関数レベル・分岐レベルで直接照合した。結果、データ比較やテストでは絶対に見つからないバグが2件見つかった。

#### 全体対応確認
- VBA 84関数 → 全てPython側に対応あり（未対応ゼロ）

#### 高リスク5関数（分岐レベル突合せ済み）

| 関数 | 結果 |
|--|--|
| CalculateDeliveryDate | 全分岐一致（意図的改善4件） |
| CreateDeliveryReport | 全分岐一致 |
| ExtractBunnoInfo | **バグ: 「ケース」が単位リストから漏れ** → 修正済 |
| ExtractApproxDelivery | Python改良のみ |
| BuildEmailBodyHTML | **バグ: 欠品確定納期がメールに未反映** → 修正済 |

#### 中リスク10関数（分岐レベル突合せ済み）
- 送り状4関数: 全一致
- 履歴保存3関数: 全一致
- 担当者振り分け3関数: 全一致

#### 意図的な差異（CLAUDE.md記載済み）
- A: 全角半角正規化
- B: 紐付き+受注先≠出荷先で出荷予定表示（VBAバグ修正）
- C: 他支店在庫の優先順位変更
- D: 15時カットオフ統一

### 6. バグ修正2件

**修正1: 「ケース」漏れ（bf3888d）**
- bunno.pyの_QTY_UNITSに「ケース」追加
- 分納で「10ケース 12/20」と入力された場合に正しくパースされない問題

**修正2: 欠品確定納期の未反映（8f60861）**
- StockoutEntryにdeliveryフィールドを追加
- メールの欠品セクションを2分岐→3分岐に修正
- 確認中一覧に手入力された確定納期がメールに表示されるようになった

## 2/19の作業内容

### 1. 特殊記号の運用分析
- Claude Codeでコードを分析し、通常の注文では人の入力が一切不要であることを確認
- 特殊記号が必要なのは例外8パターンのみ（&&, @@, ##, $$, 分納, 欠品, 引取, 確認中一覧の手入力）
- 市販ERPにもない機能を記号4つで実現しており、運用設計は良好と判断
- 特殊記号の変更・日本語エイリアス対応は不要と結論

### 2. PyInstaller exe化（324ba87, 3c83c21）
- run_gui.pyをエントリポイントとして作成
- PyInstallerで1ファイルexe（15MB）をビルド
- Python本体 + openpyxl + xlrd + tkinter + win32com を同梱
- ダブルクリック → ファイル選択ダイアログ → GUI起動の流れ
- ネットワークパス上のXLS読み込み（5,060件）、マスター参照、送付履歴（3,788件/3.56秒）全てOK
- コンソールに「納期回答書作成ツール（Python版）」「SAPの受注一覧を選択してください...」を表示
- exeを `\\flsv04\316京葉\納期回答書ツールフォルダ` にコピーして配布
- exe名を「納期回答書作成.exe」に変更（2/24。nouki_kaitou.spec の name を変更して再ビルド）
- テザリング環境ではネットワーク上のexe起動が遅い（ローカルは問題なし）→ 社内LANで要確認
- 京葉2課へ連絡済み（バッチファイルではPython未インストールPCで動かず、exeに変更）
- 再ビルドコマンド: `cd C:\Users\kento.kashiwabara\nouki_kaitou && pyinstaller nouki_kaitou.spec --clean --noconfirm`

### 3. 土日祝の納期計算修正（6f678e7）
- 藤井課長の質問「展示会等で土曜日在庫販売計上した場合も休日計算してくれるんですか？」が発端
- 修正前: 土曜10時計上 → 月曜配達予定（間違い。倉庫は土曜出荷できない）
- 修正後: 土曜計上 → 翌営業日（月曜）受注扱い → 火曜配達予定
- delivery_calc.py `_check_stock_completed` の先頭で、reg_dateが土日祝なら翌営業日に補正
- テスト7件追加、全928テスト合格

### 4. 社外コメントの備考欄反映の調査
- Claude Codeで実データ分析: 10,204件中、社外コメントあり533件（5.2%）
- うち52%は送り状番号のみ（別欄で表示済み）、47%が営業所止め・AM着希望等
- 備考欄（K列）は全角13文字が上限。それ以上は文字が縮小される
- 列を広げると他列に影響するためExcelでは限界
- 方針: コメント明細が空の場合に、社外コメントから送り状・引取を除いた残りを反映
- **実装はまだしていない。検討段階。**

### 5. 藤井課長への返信メール作成
- 土曜の休日計算: 「翌営業日の受注扱いで計算します」（最初からそうなっている体で回答）
- 社外コメント: 「別欄で表示済みの送り状・引取を除いた残りを備考欄に反映可能。ただし備考欄が狭く13文字超えると縮小。Excelではこれが限界」
- Python移植: 備考欄の話の延長で自然に触れた。「VBAマクロのPython移植が完了、将来的にはWeb化も見据えている。Web化すればレイアウトの制約がなくなり、顧客ごとにブラウザで確認できる仕組みも作れる」
- メーカー返信入力の件（簡単にならないか）: どうしようもないため触れず
- エクスポート不要の話: SAPの制約で残るので触れず

## 2/21の作業内容

### 1. VBA→Python 移行漏れ3件の修正

Claude Codeで全83関数の突合せを再実施し、接続漏れ3件を発見・修正。

| # | VBA関数 | 問題 | 修正 |
|--|--|--|--|
| 1 | rep_master_ws参照 | 担当者マスターシートの参照がPython側で未接続 | customer.pyに接続追加 |
| 2 | clean_old_history / clean_old_confirming_list | 180日超の古い履歴データの自動削除がPython側で未実装 | history.pyに実装追加 |
| 3 | is_file_open | ファイルロックチェック（送付履歴.xlsxが開いていたらエラー）が未実装 | file_utils.pyに実装追加 |

テスト全通過。これらはデータ比較では見つからない種類のバグ（日常運用で180日後に初めて顕在化する等）。

### 2. exe配布の最適化
- PyInstallerのexeサイズ最適化（32MB → 13MB）
- ツールフォルダの自動検出修正
- ハイパーリンクのスタイリング修正
- ファイルサーバー上への配置と動作確認

### 3. 運用マニュアル（PowerPoint）の作成 — Manus活用

他営業所への展開用に、業務担当者向けの運用マニュアルをPowerPointで作成。Claudeでプロトタイプ → Manusで本番版の流れ。

#### 対象読者の再定義
- PCに詳しくないが、SAPは毎日使っている営業所の業務担当者
- ツールの効果（200分→10分）は知っている前提。「どう使うの？」を知りたい人

#### 核心メッセージ
マニュアルの一番大事なポイントは「なぜ確認中になるか」「どうすればいいか」の理解。ルールの羅列ではなく、業務の感覚で「なぜそうなるか」を説明する方針。

#### Manusへの指示書（manus_instructions_final.md）
- GitHubリポジトリ（https://github.com/kento1984/nouki_kaitou）のコードを読んで自分で理解してもらう
- 「自分が業務担当者だったらどう説明されたら腹落ちするか」を考えながら作る
- 画像はNANOバナナで仮生成。実画像は後で差し替え
- 「コードからは読み取れない業務知識」セクションを追加（コード解釈の誤りを防止）

#### コードからは読み取れない業務知識（重要）
Claude Codeで保持日数の参照箇所を調査し、以下を確定:

**保持日数（顧客マスターC列）:**
- 路線便の配送日数ではない。一切関係ない。
- 意味: お客様の要望で、回答済みの伝票を回答書に数日間載せ続けるための設定。
- コード上の参照箇所はhistory.py:184の1箇所のみ（スキップ判定）。
- 納期計算（delivery_calc.py）では一切使われていない。

**路線便フラグ（顧客マスターD列）:**
- 回答書の表現が変わる。自社便 →「配達予定」、路線便 →「出荷予定」。
- 理由: 自社便は翌日届けられるから「配達」と言い切れる。路線便は運送会社任せでいつ届くかわからないから「出荷」としか言えない。

**直送と紐付きの加算日数の違い:**
- 直送 = メーカー → 直接お客様。自社倉庫を経由しない。加算日数は一切関係ない。
- 紐付き = メーカー → 自社倉庫（センター） → お客様。メーカー出荷日＋加算日数で配達予定日を計算。
- 加算日数はセンター入れ（紐付き）だけに使われる。

**在庫販売の自動計算:**
- 処理完了が前提。処理完了していれば15時基準で自動計算。
- 例外: 先の日に届けたい場合だけ、SAPの指定納期に配達日を入れる。
- 全受注の約半分が在庫販売なので、業務の大部分は何もしなくていい。

**12/31のデフォルト値:**
- SAPで受注入力時、納期未定なら指定納期に12/31が入る。
- ツールはこれを「未定」と判断し、確認中になる。

#### Manus v4（23枚）のレビュー結果
以下の間違い・改善点を発見:
- スライド6: 直送に加算日数を適用していた（間違い。センター入れのみ）
- スライド6: 在庫販売の説明に「保持日数で計算」と記載（間違い。保持日数は納期計算に無関係）
- スライド4: B列が「出荷曜日」→ 正しくは「配送可能曜日」
- スライド7: Manus向け指示書がそのままスライドに → 削除
- スライド9: 「なぜ自動か」の説明不足＋トーンが得意げすぎ
- 全スライド: ダーク背景 → ライト背景に変更
- 全スライド: 英語併記削除
- Before/Afterの人物イラスト削除

#### Manus v5（22枚）のレビュー結果
ほぼ全修正反映済み。残り2点のみ:
- スライド12: 「Tool Run」がまだ英語 → 「ツール実行」に
- スライド9: STOPマークがまだ残っている → 普通の注意書きでいい

v5でほぼ完成。内容的にもデザイン的にも業務の人に見せられるレベル。

## 2/22の作業内容

### 1. パフォーマンス改善①: GUI→run間のマスター重複読み込み解消（a8bbfca）

Claude Codeのパフォーマンス調査（9分52秒）で、_show_gui()とrun()でマスターファイルを2回読み込んでいることが判明。

**修正内容:**
- _show_gui()の戻り値を(gui_result, preloaded)のタプルに変更
- preloaded辞書にsource_data_raw, cols, orders, mfg_wb, cust_wb, tool_folder, branchを格納
- run()にpreloaded引数を追加。GUI経由時はそこからデータを取得し、CLI経由時は従来通り自前で読み込む
- キャッシュはconfirming_wsが必要なためrun()で再構築（低コスト）

**効果:**

| ファイル | 削減時間 |
|--|--|
| メーカー一覧.xlsx 2回目 | -93ms |
| 顧客マスター_v2.xlsm 2回目 | -57ms |
| SAP受注一覧 2回目 | -779ms |
| 合計 | 約929ms短縮 |

テスト928件全パス。

### 2. パフォーマンス改善②: 送付履歴バッチ書き込み（save_history_batch導入）

Claude Codeの調査で最大のボトルネックが判明: 送付履歴.xlsx（44K行）の`load_workbook(read_only=False)`に13.8秒かかっていた。さらに、同一シートを1回の処理で2-3回全行読み書きしていた。

**根本原因:**
- openpyxlがread_only=Falseでxlsxを開くと、セルのスタイル・書式・テーブル定義を全パースする
- しかし実際のコードは毎回全セルをクリアして書き戻す設計のため、パースは無駄
- save → clean → またsaveの連鎖で、44K行を2-3回読み書きしていた

**修正方針: Read fast, Write fresh + インメモリ統合**
1. read_only=Trueで読み込み、全行データをlist[list]としてメモリに保持
2. 5つの関数（save_delivery_history, clean_old_history, clean_old_confirming_list, save_confirming_list, clean_confirming_list）の処理をすべてメモリ上のlistに対して実行
3. 全操作完了後、initialize_delivery_historyで新しいxlsxを生成し、1回だけ書き出す

**変更ファイル:**
- history.py: extract_sheet_rows, save_history_batch, _write_history_workbook, _filter_old_rows を追加
- main.py: import変更、read_only wb閉じる前にextract_sheet_rowsで行データ保持、save_history_batch 1回に置き換え
- tests/test_history.py: 11テスト追加（TestExtractSheetRows 4件 + TestSaveHistoryBatch 7件）

**効果（実測値）:**
- 送付履歴保存: 約20秒 → **2.67秒**（85%以上短縮）
- load_workbook(read_only=False)の13.8秒が完全に不要に
- セルからの行データ再読み取り（2-3回×44K行）がゼロに

テスト940件全パス（928 + 12件追加）。Excelで開いてテーブル・ドロップダウン・色分けが正常であることも確認済み。

**軽微な動作差異（改善方向）:** 旧コードではall_confirmedが空の場合、clean_old_historyとclean_old_confirming_listが実行されなかった。新コードでは常に古いレコードを除去する。

### 3. 確認中一覧の色分け追加: 一部処理済み（42e7e56）

確認中一覧シートで「一部処理済み」ステータスに色がついていなかったため、薄緑（#C8FFD4）を追加。

**変更:**
- excel_writer.py: `_CONFIRM_PARTIAL = ("C8FFD4", None)` 追加、color_confirming_listのelifチェーンに追加
- tests/test_excel_writer.py: test_partial テスト追加

テスト940件全パス。

### 4. 送り状の数量表示の正規化（606235f）

回答書・メールの送り状セクションで、数量が「x1.00」「x1」のように不統一だった問題を修正。SAPから来る数量データの型（整数/小数）の違いが原因。

**修正内容:**
- utils.py: `format_quantity()` 関数を追加（Decimal.normalize()で末尾ゼロ除去）
- excel_writer.py: 送り状・欠品・分納・分納完了の4箇所で `x{qty}` → `x{format_quantity(qty)}` に変更
- email_builder.py: 同じ4箇所を変更
- tests/test_utils.py: TestFormatQuantity 12テスト追加

**効果:** 1.00→1、2.50→2.5、100.00→100 のように末尾の不要なゼロを除去。テスト全パス、コミット＆プッシュ＆再ビルド済み。

### 5. 一部処理済みの色分け修正

実データで色分けが効いていないことが判明。原因はSAPのステータス値が「一部処理済」（「み」なし）で、コード側は「一部処理済み」で判定していたため。1文字の差異を修正し、どちらでも色がつくように対応。

### 6. 分納・欠品のライフサイクル網羅検証

分納・欠品は発生頻度が低く実データでのテストが難しいため、コードの全分岐を洗い出し、テストカバレッジを検証。

**実施内容:**
- 分納・欠品の状態遷移図（ライフサイクル）を作成
- 全パターンのテストカバレッジ対応表を作成
- テスト18件を追加（953件 → 971件）
- プロダクションコードの変更なし

**分納のテストカバレッジ（B1〜B14）:** 全14パターンカバー済み。今回追加分:
- B10: 既存分納(keep)+未定あり→確認中に留まる
- B11: 既存分納(keep)+全確定→送付履歴「分納完了」
- B12: 一部処理済み+分納→未処理と同じ扱い
- B13: is_bunno_in_confirmingがforce_deliveredをブロック
- B14: 分納+欠品併存→欠品収集から除外

**欠品のテストカバレッジ（S1〜S12b）:** 全12パターンカバー済み。今回追加分:
- S7: 欠品+日程調整中→「欠品中」に置換
- S9: 欠品+確定日手入力→overrideスキップ
- S10: 欠品+処理完了→欠品情報収集除外
- S12: 欠品+直送処理完了→force_delivered「納品済み」

**結論:** 分納・欠品の処理ロジックに問題なし。全パターンがテストでカバーされていることを確認。

### 7. 分納・欠品の運用フロー解説の作成

Claude Codeに分納・欠品それぞれの運用フローを業務目線で解説させ、業務担当者向けのHTML資料として整理。

**作成した資料:**
- 欠品の運用フロー.html
- 分納の運用フロー.html

**欠品のライフサイクル（まとめ）:**
- 入力: SAPのコメント（明細）に「欠品中」と書く
- 確認中一覧に登録: ステータス「欠品中」、薄紫色
- 解消パターン:
  - パターンA: 確認中一覧のJ列に確定納期を手入力 → 通常の納期計算で確定 → 送付履歴に移動
  - パターンB: 直送で処理完了 → force_deliveredで「納品済み」→ 送付履歴に移動
  - パターンC: 紐付きで処理完了 → 通常の納期計算 → 送付履歴に移動
  - パターンD: 180日超過 → 自動除去（安全弁）

**分納のライフサイクル（まとめ）:**
- 入力: SAPのコメント（明細）に「分納:数量 日付 場所,数量 日付 場所」と書く
- 未定がある場合のみ確認中一覧に登録: ステータス「分納」、薄青色
- 全確定の場合は直接送付履歴に「分納完了」で記録
- 一部処理済みは未処理と同じ扱い
- 日程確定方法: SAPのコメント更新 or 確認中一覧のJ列に手入力
- 分納完了時: メールに緑の囲みで「分納完了のご連絡」（1回だけ表示）

### 8. 納期計算ルールの確認

分納・欠品で入力した日付が、直送/紐付きのルールに基づいて正しく納期計算されているかをClaude Codeで確認。

**結論:** 3つの入力方法（SAPコメントの分納日付、確認中一覧J列の分納確定日、確認中一覧J列の欠品確定日）すべてが同じルールで納期計算される。

| 伝票タイプ | 計算 | 表示 |
|--|--|--|
| 直送（転送中） | 入力日そのまま | 「○月○日出荷予定」 |
| 紐付き・自社便 | 入力日 + 配送加算日数 | 「○月○日配達予定」 |
| 紐付き・路線便 | 入力日 + (配送加算日数-1) | 「○月○日出荷予定」 |

入力した日付がそのまま「○日配達です」と表示されることはない（直送の出荷予定を除く）。紐付きでは必ず配送日数が加算される。この計算ルールはコードに正しく実装されており、CLAUDE.mdに追記する必要なし（業務知識は既に記載済み）。

### 9. CLAUDE.mdの更新

「ビルド」セクションを追加。コード変更後は必ず `python -m PyInstaller nouki_kaitou.spec --clean --noconfirm` で再ビルドすること。Claude Codeが再ビルドを忘れる事故を防止。

### 10. 網羅的な影響確認

今日の全変更が既存機能を壊していないか、Claude Codeで網羅的に検証。

- 全971テスト全パス
- CLI経由（preloaded=None）: 従来通り動作確認
- GUI経由（preloaded あり）: 正常動作確認
- save_history_batchと旧5関数の等価性: 全項目一致

**色分け一覧（最新）:**

| ステータス | 色 | カラーコード |
|--|--|--|
| 出荷完了 | 薄ピンク | #FFB4B4 |
| 一部処理済み / 一部処理済 | 薄緑 | #C8FFD4 |
| 欠品中 | 薄紫 | #DCC8FF |
| 分納 | 薄青 | #C8DCFF |
| 7日以上経過 | オレンジ | #FFC896 |
| 3日以上経過 | 黄色 | #FFFFC8 |

### 11. 会議準備の検討

**会議の概要:**
- 時間枠: 1時間（実質20-25分で終わる想定）
- 参加者: 業務担当者15人
- 目的:「怖くない」「自分にもできる」を持って帰ってもらう

**推奨進行（20-25分）:**
1. デモ（5分）— SAPエクスポート→exe起動→回答書生成を実演
2. 解説（10-15分）— 22枚中6枚のみ使用（スライド8,10,12,9,20,21）
3. 質疑（残り全部）

**非技術者向けQ&A:**
- 「exeって何？」→「WordもExcelもOutlookも全部exe。同じもの。」
- 「Pythonって何？」→「VBAと同じプログラミング言語の一つ。やっていることはマクロと同じ。」
- 会議では「exe」ではなく「ソフト」「ツール」で統一

**セキュリティ面の回答準備:**
- インターネットに一切つながらない
- やっていることはExcelマクロと同じ（SAPデータ読み取り→Excel生成→Outlook起動）
- ソースコード全量がGitHubにあり、中身が100%見える。自作だから怪しいコードが混入する余地がない
- WindowsのSmartScreenが「不明な発行元」と警告を出す可能性 → デジタル署名がないため。IT部門に事前確認 or 「詳細情報→実行」で通す

**ウイルス対策ソフトの事前確認が重要。** 当日「動きません」が連発すると会議が詰まる。情シスに事前に一声かけるか、自分のPC以外で1台試しておくこと。

**exeの配布方法:**
- ファイルサーバー `\\flsv04\316京葉\納期回答書ツールフォルダ` にexeを置く
- 既にマクロでフォルダを持っている人は、exeをそのフォルダに入れるだけ
- マスターファイル名（メーカー一覧.xlsx、顧客マスター_v2.xlsm、送付履歴.xlsx）が揃っていればフォルダ名は何でもいい
- 受注一覧はどこに保存してもいい（実行時にファイル選択ダイアログで選ぶ）
- 送付履歴だけはファイルサーバー上のものを使う必要がある（全員で共有するため）

## 2/23の作業内容

### 1. 運用マニュアルv6のレビュー

Manusが黒板デザイン（Nano Banana Pro風）で作成したv6（22枚）をレビュー。以下の問題を発見：

**誤植・未修正:**
- スライド18: タイトルが「欠品の全む欠品の全体フロー」→「欠品の全体フロー」が正しい
- スライド19: STOPマークが残っている（v5→v6で削除指示済みだが未反映）

**英語の混入:**
v5にはなかった英語が15箇所以上追加されていた（Partial Delivery, Stockout, Exclude, Price Confirm, Work Slip, Symptom, Cause, Solution, Method, Section, Logic, No space!, Run ▷）。業務担当者15人向けの資料なので全て日本語にすべき。

**内容の正確性:**
- スライド6: 在庫販売の説明が「即納」→ 不正確。15時基準＋配送可能曜日の計算がある
- スライド1: 「1. 初めての方へ」「2. 活用編」の目次構造がv6で追加されたが不要

**デザイン:**
- スライド17-18の背景が他のスライドと異なる（木枠風要素あり）→ 統一すべき

### 2. スライド順番のハンズオン向け再構成

現在の順番（設定→運用）をハンズオン形式に再構成：
1. つかみ＆デモ（表紙、200分→10分、3ステップ ← ここでライブデモ）
2. 日常業務の理解（在庫販売の安心感、確認中の説明、解消方法）
3. SAP入力ルール（コメント3種類、分納・欠品、社外コメント、制御記号）
4. 特殊ケース（分納フロー、欠品フロー、除外）
5. セットアップ＆リファレンス（フォルダ構成、顧客マスター、メーカー一覧、配送加算日数、送付履歴）← 巻末
6. まとめ（トラブル、チェックリスト、5つのポイント）

### 3. スクリーンショット枠の分割指示

図解と実画面プレースホルダーを1枚に押し込んでいるスライドが10枚あり、プロジェクター投影時に見にくい。「図解スライド」と「実画面スライド」の2枚構成に分割する方針を決定。22枚→32枚前後になる想定。

### 4. Manus v7修正指示書の作成（manus_v7_revision_instructions.md）

上記1-3の全修正項目をまとめた指示書を作成。v6のPDFとv5のPDF（英語がない正確な表現の参考用）を添付してManusに再修正依頼。

### 5. 会議進行の決定事項

- 初回はデモのみ（全員操作はしない）。翌日から各自実運用
- exeは賢人さんのPCで問題なく起動済み（ウイルスソフトのブロックなし）

### 6. 業務ルール確認表 vs コード突合せ

Claude Codeに業務ルール確認表.xlsx（4シート46ルール+73具体例）とPythonコードの全分岐を照合させた。

**結果:**
- 納期計算ルール: 45行中11件の不一致
- 具体例: 73件中72件OK
- 用語集: 28用語中1件の不一致
- 凡例: 7カテゴリ、問題なし

**高リスク（1件）:**
- ルール4-10: 紐付き+指定納期+処理完了で「処理完了パスへ」と書いてあるが、実際は指定納期パスが優先される

**コードバグ発見（2件）:**

**バグ1: _resolve_delivery_placeの正規化漏れ**
- report_generator.py:595で `delivery_place == row.customer_name.strip()` と直接比較
- 他の5箇所では `normalize_name_for_comparison()` を使っているが、6箇所目が漏れていた
- 全角（有）と半角(有)の揺れがあると納入先名が「貴社」にならない

**バグ2: _calc_normalに紐付き+受注先≠出荷先の出荷判定がない**
- `_check_specified_date`（指定納期パス）には `is_himozuki_diff` のロジックがある
- `_calc_normal`（受注納期パス）には同じロジックがなかった
- 紐付き+受注先≠出荷先+受注納期パスで「配達予定」と出てしまう（本来は「出荷予定」）

### 7. コードバグ2件の修正

両方のバグをClaude Codeで修正。

**バグ1修正:**
- report_generator.py:594-596: `normalize_name_for_comparison()` で比較に変更
- CLAUDE.md: 「5箇所」→「6箇所」に更新
- テスト2件追加

**バグ2修正:**
- delivery_calc.py:705-724: `_calc_normal` に `is_himozuki_diff` パターンを追加
- テスト2件追加

テスト全975件パス（971→975）、PyInstallerビルド成功。

### 8. 全分岐比較（_check_specified_date vs _calc_normal vs _check_dec31）

バグ2の修正に際し、指定納期パスと受注納期パスの他の分岐にも同様の漏れがないか、3つの関数の全8分岐パターンを比較。

| # | 分岐条件 | 指定納期 | 受注納期 | 確定日 | 差分 | 判定 |
|--|--|--|--|--|--|--|
| 1 | 在庫+転送中 | そのまま→出荷 | そのまま→出荷 | そのまま→出荷 | なし | OK |
| 2 | 在庫+受注先≠出荷先 | 逆算→出荷 | そのまま→出荷 | そのまま→出荷 | あり | 意図的 |
| 3 | 在庫+自社便 | 加算なし→配達 | 加算あり→配達 | 加算あり→配達 | あり | 意図的 |
| 4 | 直送+転送中 | そのまま→出荷 | そのまま→出荷 | そのまま→出荷 | なし | OK |
| 5 | 紐付き+曜日あり | 加算→曜日→出荷 | 条件分岐あり | 加算→曜日→出荷 | あり | 意図的 |
| 6 | 紐付き+路線便 | days-1→出荷 | days-1→出荷 | days-1→出荷 | なし | OK |
| 7 | 紐付き+受注先≠出荷先 | 加算→逆算→出荷 | 加算→逆算→出荷 | 加算→出荷 | なし | OK（今回修正） |
| 8 | 紐付き+自社便 | 加算→配達 | 加算→配達 | 加算→配達 | なし | OK |

差分3件は全て意図的な設計差異（指定納期=到着日、受注納期=出荷日という日付の意味の違い）。追加修正は不要と確認。

### 9. 業務ルール確認表の修正・再生成

`dev_scripts/generate_business_rules.py` を14件修正して `業務ルール確認表.xlsx` を再生成。

| # | ルール | 修正内容 |
|--|--|--|
| 1 | ルール1 (&&作業) | 回答結果: 「受注納期」→「指定納期→受注納期の順で取得」 |
| 2 | ルール2 (@@着日) | その他条件: 「コメント(社内)」→「コメント(社外+社内の結合文字列)」 |
| 3 | ルール3 (引取) | 同上＋回答結果: 「受注納期」→「引取日」 |
| 4 | ルール4-8 | 曜日制限の適用を追記 |
| 5 | ルール4-10 | 「処理完了パスへ」→「指定納期パスで処理」（**高リスク修正**） |
| 6 | ルール5-1 | 「翌営業日」→「他拠点より出荷予定/済み」 |
| 7 | ルール5-3 | 備考にuse_ship_rule vs is_himozuki_diffの実装差異を追記 |
| 8 | ルール6-1 | 転送中の分類を明確化 |
| 9 | ルール6-3 | 備考にis_himozuki_diffの詳細を追記 |
| 10 | ルール9-2 | 「逆算」→「加算日数+曜日制限」に修正 |
| 11 | ルール9-3 | 加算日数+曜日制限を追加 |
| 12 | ルール9-5 | 曜日制限＋is_himozuki_diffの説明追記 |
| 13 | ルールA-2 | 伝票タイプ: 「全タイプ」→「直送/紐付き」、is_bunno_completed条件修正 |
| 14 | 用語集 | is_bunno_completedの説明修正 |

出力先も `dev_scripts/business_rules.xlsx` → プロジェクトルートの `業務ルール確認表.xlsx` に変更。

## 2/24の作業内容

### 1. 会社PCでのgit pull＆exe再ビルド

会社PC（C:\Users\kento.kashiwabara\nouki_kaitou）でgit pull実施。.claude/settings.local.jsonのコンフリクトが発生したがリモート版を採用して解決。2/19以降の全コミット（exe最適化、バグ修正2件、テスト追加等）をFast-forwardで取得。

exe名を「納期回答書作成.exe」に変更して再ビルド。nouki_kaitou.specのnameを'納期回答書作成'に変更。dist/納期回答書作成.exeを生成。

### 2. Cドライブ容量問題の対処

**発覚:** Cドライブ174GB中、空き0バイト（深刻）。印刷スプーラーも動作不可。

**原因調査と対処:**

| 対象 | サイズ | 対処 |
|--|--|--|
| AnthropicClaude（Claudeデスクトップアプリ） | 969MB | アンインストール＆フォルダ削除 |
| Temp | 170MB | 削除 |
| .claude.json.backup.*（5ファイル） | 数MB | 削除 |
| .claude.json.corrupted.* | 数MB | 削除 |
| nouki_kaitou.zip | 数MB | 削除 |

**結果:** 0バイト → **6GB確保**。印刷も復旧。

**今後の運用ルール:**
- Claudeデスクトップアプリは会社PCに入れない（最大の犯人だった）
- 週1で掃除バッチを実行
- PyInstallerの再ビルド後はbuildフォルダを消す
- Claude Code/Cursorのキャッシュ先をDドライブに変更（未実施）

**Programs内訳（消せないもの）:**

| フォルダ | サイズ |
|--|--|
| Programs全体 | 1,649MB |
| 内訳: Python, VS Code, Notion, CybertrustDeviceiDImporter | 全て必要 |

### 3. 顧客マスター未登録時の動作確認

Claude Codeで調査。受注一覧に顧客マスター未登録の顧客しかいない場合の挙動を確認。

**結果:** エラーにはならず、3段階のフィルタで安全に除外される。

| 段階 | 場所 | 動作 |
|--|--|--|
| GUI | gui.py:427-430 | 顧客リストに表示しない（選択不可） |
| CLI | main.py:643-654 | 「顧客マスター未登録（スキップ）: N件」を出力し除外 |
| メールチェック | main.py:658-664 | 登録済みだがメールアドレスなしの顧客をprintで警告（処理は続行） |

顧客マスターに存在しない顧客の受注は回答書が一切生成されない。VBAと同じ安全設計。

### 4. 藤井課長の社外コメント要望への対応方針決定

藤井課長の2/20メールの要望：注番列を社外コメントに置き換えたい。「幅に制約あれば、マツモト注番不要」「Excelだからセル選択して確認できる」。

**課題:** 注番がなくなると困る営業所（京葉・姫路）もある。

**設計方針（Claude Codeで調査済み）:**
- メーカー一覧.xlsxの営業所設定シートにG列「remarks_mode」を追加
- `detail`（デフォルト）= 現状維持（注番表示）
- `external` = 注番の代わりに社外コメント表示（送り状番号・引取情報は自動除外）
- G列未設定の営業所は現状維持。松本営業所だけ切り替えも可能

**変更ファイル:** models.py, config.py, report_generator.py + ヘルパー関数（計約20行）

**藤井課長への返信方針:**
- 変更自体は大きな改修ではない
- ただし注番がなくなると困る営業所もあるため一律の変更は慎重に
- 営業所ごとに切り替え可能な仕組みは簡単に入れられる
- 松本営業所だけ対応することも可能
- クールに返す。「承知しました」ではなく「確認しました」的なトーンで検討感を出す

### 5. GUI小画面対応（d0e81ad）

1366x768の画面でGUIウィンドウ下部のOK/キャンセルボタンが切れて押せない問題を修正。

**原因:** ウィンドウ高さが770px固定。1366x768ではタスクバー+タイトルバーを引くと使える高さが約690px → ボタン（y=690〜760）が完全に切れる。

**修正内容（gui.py）:**
- `winfo_screenheight()`で画面高さを検出
- `win_h = min(770, screen_h - 100)`で動的にウィンドウ高さを決定
- 顧客リスト/伝票番号入力エリアを縮めてボタンを必ず表示
- 10行追加、7箇所の固定値を動的計算に変更

| 画面 | ウィンドウ高さ | 顧客リスト高さ | 変化 |
|--|--|--|--|
| 1920x1080 | 770px（従来通り） | 414px | 変更なし |
| 1366x768 | 668px | 322px | -92px（十分使える） |

### 6. 顧客マスターのロック問題の発見と解決

ビルド後のexeで「該当するデータがありません」と表示され顧客リストが空になる問題が発生。

**調査経緯:**
1. gui.pyの小画面対応を疑いrevert → 同じ症状 → gui.pyは無関係
2. main.pyにデバッグprint追加 → `orders=46,661件, master_customers=0件`
3. 顧客マスター_v2.xlsmのシート名・先頭行確認 → ヘッダーは読めるがデータ行が全てNone
4. ローカルの顧客マスターに切り替え → `master_customers=83件` で正常動作

**原因:** ネットワーク上の顧客マスター_v2.xlsmを誰かがExcelで開いていた。openpyxlはロック中のxlsmを開くとセルの値がNoneになる。

**対応:** コードの問題ではないため修正不要。同じ症状が出たら「顧客マスターを閉じてもらう」で解決。

### 7. 数量列の小数表示修正（c50dee9）

納期回答書の数量列で12.5が13に四捨五入表示される問題を修正。

**原因:** `_to_numeric()`は正しく12.5（float）を返していたが、Excelのセル書式が`#,##0`（整数表示）に固定されていたため、表示時に四捨五入されていた。

**修正:** excel_writer.pyの2箇所（`_apply_number_formats`と確認中一覧の書式設定）で、数量が小数の場合は`#,##0.###`書式を使うように変更。整数なら従来通り`#,##0`。

### 8. 送り状キーワードの追加（ededfc0）

松本営業所の社外コメント分析で、キーワードリスト未登録の運送会社を発見。tracking.pyに5社追加。

| キーワード | 正式名称 |
|--|--|
| 福通 | 福山通運（「福山」は既存、「福通」が略称で未登録） |
| 近物 | 近物レックス |
| 新潟 | 新潟運輸 |
| 名鉄 | 名鉄運輸 |
| エクスプレス | セイノースーパーエクスプレス（「セイノー」「SSX」は既存） |

### 9. 松本営業所の社外コメント分析

松本営業所の受注一覧（42,111件）のコメント（社外）列を全件分析。藤井課長の社外コメント表示要望に対する安全性検証。

**データ概要:** 42,111レコード中、社外コメントあり10,109件（3,758種）

**パターン分類:**

| パターン | 件数 | 割合 |
|--|--|--|
| 送り状番号付き | 3,653 | 36% |
| 運送会社名のみ（番号なし） | 1,525 | 15% |
| 点検修理 | 933 | 9% |
| 顧客名（○○様） | 712 | 7% |
| 分納 | 647 | 6% |
| ○月伝票 | 541 | 5% |
| 欠品 | 109 | 1% |
| 出荷元（○○より） | 83 | 1% |
| 特価 | 50 | 0.5% |
| 保留 | 30 | 0.3% |
| その他 | 1,535 | 15% |

**検証結果:**

| 項目 | 結果 |
|--|--|
| 社内情報漏洩リスク | なし（現在のコードでは社外コメントは回答書に非表示） |
| 送り状フィルタの誤爆 | なし |
| 引取フィルタの誤爆 | 1件注意（「引き取り番号S4-25046096」が引取として誤検出） |
| 送り状検出漏れ | あり → キーワード追加で対応済み（項目8） |

**コードの設計:** 社外コメントは送り状番号抽出と引取判定の入力としてのみ使用。「保留」「特価」「翌月請求」等が書いてあっても回答書には一切漏れない。

**藤井課長の懸念に対する結論:** 社外コメントを表示する機能を実装しても、入力方法は一切変わらない。今まで通り自由に書くだけ。特殊記号等の新しいルールは不要。

### 10. 藤井課長への返信メール送信・返信受領

朝9:07に返信メール送信済み。同日、藤井課長から返信あり。

**藤井課長の返信要旨:**
- 社外コメント表示は、柏原の工数の小さい方でよい
- 「メーカーからの返信入力が熟練を要する業務（＝特殊記号使用）にさえならなければ」
- 明日の会議での皆の意見に従う

**藤井課長の懸念の本質（分析）:**
社外コメントを表示するために、社外コメントの書き方に新しいルール（特殊記号等）が設けられることを心配している。松本のメンバーは社外コメントを自由に書いており、それが「熟練を要する業務」になることへの危惧。実際にはコードの設計上、入力方法に変更は不要。

### 11. 藤井課長・下川係長とのやりとり分析

2/4〜2/20の約3週間のメールやりとり全体を分析。

**藤井課長の特徴:** 思いつきで質問を連発、1通に5-6個詰め込む。「フライング藤井です」と自覚あり。技術では勝てないので組織ルートで存在感を出そうとしている（「4月新システムの改善要望として提出した」）。

**下川係長の特徴:** 正論で組織の建前を押し付ける（会社指定フォーム、FAX対応）。1通目で5つ質問→全部的確に返されて→2通目で「しばらく使ってみます」で撤退。

**賢人さんの戦い方:**
- 全部に答えた上で、自分のペースは絶対に崩さない
- 優先順位は自分が決める。答えないことは自然にスルー（メーカー返信入力、エクスポート不要の話）
- 先手を打って恩を売る（頼まれる前に路線便対応・担当者別分割を実装）
- 技術的な余裕で主導権を維持

## 2/25の作業内容（家のPC）

### 1. 家のPCでgit pull＆再ビルド

C:\repos\nouki_kaitouでgit pull実施。Fast-forwardで4ファイル更新（excel_writer.py, gui.py, nouki_kaitou.spec, tracking.py）。コンフリクトなし。`python -m PyInstaller nouki_kaitou.spec --clean --noconfirm` で再ビルド成功。

### 2. SAPの2行レコード問題の調査

SAPの受注一覧（10PM.XLS）で1レコードが2行に分割されるケースの処理を調査。

**調査結果:**
- VBA・Python両方とも「副行スキップ」方式で対応済み
- 受発注伝票列に値がある行 → データ行（処理する）
- 受発注伝票列が空の行 → 副行（スキップする）
- 副行に格納されているのはマツモト側の営業担当者名のみ（業務ロジックでは未使用）
- 3行以上に折り返されてもスキップ方式なら壊れない
- VBAもPythonも同じ設計。実害なし。

### 3. parse_date()に月/日のみ入力（M/D形式）の対応追加（2dec631）

確認中一覧のJ列に `3/1` のように年なしで日付を手入力した場合、parse_date()が認識できず静かに無視されていた問題を修正。

**問題:**
- VBAのCDateは `1/5` で今年を自動補完する
- Python版のparse_date()にはその補完ロジックがなかった
- エラーも出ずNoneが返るため、入力者は反映されたと思い込む → サイレント不具合

**修正内容（utils.py）:**
- `(\d{1,2})/(\d{1,2})$` の正規表現でM/D形式をマッチ
- 今年を補完し、補完した日付が今日より過去なら翌年にする
- テスト用に `today` キーワード引数を追加（省略時は `date.today()`）
- テスト7件追加、981件全パス

### 4. parse_date()にExcelシリアル値への対応追加（05ef812）

確認中一覧のJ列に `3/1` と入力すると、Excelが日付と認識して内部的に46082（シリアル値）に変換。openpyxlで読むと数値の46082が返り、parse_date()が認識できなかった。

**修正内容（utils.py）:**
- int/float型で1〜2958465の範囲なら、Excelシリアル値として日付に変換
- `datetime.date(1899, 12, 30) + timedelta(days=value)` で変換
- テスト6件追加、987件全パス

**項目3と4の関係:**
- `3/1` が文字列のまま残った場合 → 項目3のM/D形式で対応
- `3/1` がExcelに46082に変換された場合 → 項目4のシリアル値で対応
- 両方カバーしたのでどちらのケースでも動く

### 5. 確認中一覧J列→納期計算の処理フロー確認

確認中一覧のJ列に手入力した出荷日が、紐付きの場合に配送日数の加算が正しく行われるかをClaude Codeで検証。

**確認結果（コードレベルで検証済み）:**

| ケース | 計算 | 表示 | delivery_calc.py | bunno.py |
|--|--|--|--|--|
| 直送（転送中） | そのまま | 出荷予定 | 634-635行 | 544-547行 |
| 紐付き・自社便 | +加算日数 | 配達予定 | 647-657行 | 556-561行 |
| 紐付き・路線便 | +(加算日数-1) | 出荷予定 | 640-644行 | 548-554行 |

delivery_calc.py（通常の12/31受注納期）とbunno.py（分納の未定/○旬予定）の両方で同じ3分岐ロジックが実装されている。

### 6. 路線便の網羅的検証＆テスト追加（6f32d60）

路線便（is_rosenbin）は使用頻度が低くテストが不足していたため、全パスを検証。

**路線便フラグの読み取り経路:**
顧客マスターD列「○」→ cache.py:142-144 → customer.py:116 `is_route_delivery()` → delivery_calc.py:294 `is_rosenbin` → delivery_calc.py:298-299 `use_ship_rule`上書き

**全6パスで「出荷予定」確認済み。** `days_to_add - 1` は5箇所に存在:
- delivery_calc.py:477 (指定納期)
- delivery_calc.py:642 (12/31確定)
- delivery_calc.py:699 (通常計算)
- bunno.py:551 (分納+確定日)
- bunno.py:580 (分納+M/D日付)

**テスト3件追加（既存9件→12件）:**

| テスト | パス |
|--|--|
| test_stock_rosenbin_specified | 在庫+路線便+指定納期 → 1営業日逆算で出荷予定 |
| test_stock_rosenbin_completed | 在庫+路線便+処理完了 → use_ship_rule経由で出荷済み |
| test_himozuki_rosenbin_specified | 紐付き+路線便+指定納期 → days_to_add-1で出荷予定 |

テスト990件全パス。

### 7. 会議準備（マクロからPythonにした理由の整理）

2/25の業務会議でマクロ経験者から聞かれる想定で、Python移植の理由を整理。

**一番シンプルな答え:** 「VBAではWeb化できないから」

**実績で示す答え（最強）:** 「マクロのままじゃexeにして全営業所に配れなかった。Python化したから今こうやって配布できてる」

**技術者向け:** 「テストが書ける。VBAで7,000行を変更するのが怖くなかったか？Pythonなら990件のテストが見張ってくれる」

**テストについての補足:**
- テストはClaude Codeが作成。「テスト追加して」で自動生成
- VBAにはpytestのような標準テストフレームワークがなく、テストを書くのが非現実的だった
- AIが作ったテストは「コードが今やってることを正解として書く」傾向があり、コード自体にバグがあるとテストも通ってしまう弱点がある
- だからVBAとのコード突合せ・データ比較を別途やった。テスト単体では品質保証はできない

## 2/26の作業内容

### 1. 仙台営業所の配送ルール対応（設計・実装・テスト完了）

仙台営業所（佐藤さん）から配送ルールの回答を受領。京葉・姫路とは根本的に異なる複雑な配送体系（1日2便制、顧客別締切時間、当日配達）への対応を設計・実装した。

#### 仙台の配送体系（京葉・姫路との違い）

京葉・姫路は「1つのcutoff（15時）で2択（翌日/翌々日）」。仙台は「顧客ごとに異なるcutoffで最大3択（当日/翌日/翌々日）」という根本的な構造差異がある。

| パターン | cutoff1 | cutoff1前 | cutoff2 | cutoff2前 | 全超過 |
|--|--|--|--|--|--|
| 近隣2便 | 11:30 | 当日午後配達 | 16:00 | 翌日午前配達 | 翌日配達 |
| 遠方午前 | 16:00 | 翌日配達 | - | - | 翌々日配達 |
| 遠方午後 | 11:30 | 当日配達 | - | - | 翌日配達 |
| 路線便 | 15:00 | 出荷 | - | - | 翌日出荷 |

#### 設計方針（最重要）

**既存ロジックを一切変更しない。** 仙台用の配送計算を別モジュール（sendai_delivery.py）として新規作成し、営業所判定で分岐させる。京葉・姫路は今まで通り既存ロジックを通る。既存テスト990件には一切影響を与えない。

#### 実装内容

**新規ファイル（2）:**
- sendai_delivery.py — 仙台配達計算コア（check_sendai_stock_completed, _calc_pattern_days, _before_cutoff_hm）
- tests/test_sendai_delivery.py — 40テストケース（単体+結合テスト）

**変更ファイル（7）:**

| ファイル | 変更内容 | 影響度 |
|--|--|--|
| models.py | DeliveryPattern dataclass追加、CacheStoreに3フィールド追加 | 低 |
| cache.py | _detect_customer_master_format追加、build_customer_cacheを4-tuple化、build_pattern_cache追加 | 中 |
| customer.py | get_customer_pattern追加、email関数にemail_start_colパラメータ追加 | 低 |
| delivery_calc.py | Step5の前に仙台分岐を8行挿入 | 最小 |
| main.py | _build_email_customersにemail_start_colパラメータ追加 | 低 |
| email_builder.py | get_email_addresses呼び出しにemail_start_col渡し | 低 |
| tests/test_cache.py | build_customer_cacheの4-tuple対応、TestBuildPatternCache等追加 | 低 |

**テスト結果:** 1,077件全パス（既存990件 + 新規87件、0 failures）

#### 紐付き+処理完了の仙台パターン対応（追加実装）

当初は在庫販売のみの対応だったが、紐付き+処理完了（指定納期なしでいきなり処理完了になったケース）も仙台パターンで計算する必要があると判断。商品がセンターに入った時点で在庫販売と同じ状態であり、配達タイミングは仙台の便体制（11:30/16:00）に依存するため。

- sendai_delivery.pyにcheck_sendai_himozuki_completedを新規追加
- delivery_calc.pyの_check_himozuki_completed呼び出し直前に仙台分岐を挿入
- 既存の_check_himozuki_completedは一切変更なし
- テスト19件追加（フォールスルー5件、パターン別10件、受注先≠出荷先2件、曜日制限2件、路線便1件、全角半角1件）

在庫販売との違いはベースになる時刻のみ：在庫販売=登録日時（SAP出荷処理時刻）、紐付き=マクロ実行時刻。

#### 当日配達のサフィックス修正

近隣2便・遠方午後でcutoff前（当日配達）の場合、「配達済み」ではなく「配達予定」を表示するよう修正。午後便がまだ出ていない段階で「済み」は不正確。biz_days >= 1（翌日以降）は従来通り「配達済み」。在庫販売・紐付き両方に適用。テスト期待値6箇所を修正。

#### デバッグ: 仙台パターンが実行時に発動しない問題

京葉のデモ環境（メーカー一覧のGLを仙台営業所に変更、顧客マスターE列に「近隣2便」設定）でexeを実行したところ、17時台処理完了の伝票がsendai_delivery.pyの計算(+1日)ではなく従来ロジック(+2日)で回答されていた。

**原因:** `_detect_customer_master_format`がE列の最初の非空データセルに`@`が含まれるかで新旧フォーマットを判定していたが、openpyxlで`read_only=False`（実際のexeの動き）で.xlsmを開くと、メールアドレスセルのHYPERLINK数式が`mailto:xxx@...`として展開され、E列（配送パターン列）にまで`mailto:`が漏れ出す。結果、E列に`@`が含まれるため旧フォーマットと誤判定→配送パターン列を読み飛ばす→cust_patternが空→sendai関数がNoneを返す→従来ロジックにフォールスルー。

**修正（0a78dfc）:** 値ベースの`@`判定をやめ、ヘッダ行（1行目）のE列が「配送パターン」かどうかで判定するよう変更。

- 新マスター: E列ヘッダ=「配送パターン」→ True（新フォーマット）
- 旧マスター: E列ヘッダ=「メールアドレス1」→ False（旧フォーマット）

後方互換性に影響なし。テスト6ケースに更新（旧ヘッダ、新ヘッダ、空ヘッダ、短いヘッダ、mailto混在でも新フォーマット検出、空シート）。

**デバッグの教訓:** Claude Codeの最初の分析ではnouki_kaitouフォルダ（開発用）のメーカー一覧を読んでおり、デモフォルダのファイルを正しく参照していなかった（存在しない「盛岡営業所」が出力されていた）。パス指定を明示して再調査させることで正しい結果が得られた。

#### 当日配達(biz_days==0)の過去日を配達済みに修正（ad2c22c）

当日配達で一律「配達予定」としていたが、過去の当日配達分（例: 2/24計上・当日配達を2/26に閲覧）がいつまでも「配達予定」のままになる問題を修正。

- 配達日 >= 今日 → 「配達予定」（まだ届いていない）
- 配達日 < 今日 → 「配達済み」（既に届いている）

check_sendai_stock_completedとcheck_sendai_himozuki_completedの両方を修正。テスト4件追加。

#### 技術的ポイント

- **分単位cutoff:** (hour, minute)のタプル比較で対応。(11, 30) < (11, 30) → False（11:30ちょうど＝締切後）
- **当日配達（+0日）:** add_business_days(date, 0)は想定外の使い方なので、biz_days == 0の場合はadd_business_daysを呼ばずreg_dateを直接使う分岐を実装
- **2段階cutoff:** DeliveryPatternにcutoff1/cutoff2を持たせ、_calc_pattern_daysで段階的に判定

#### 後方互換性

| シナリオ | 動作 |
|--|--|
| 旧顧客マスター（E列ヘッダ=メールアドレス1） | ヘッダ判定→旧フォーマット→cust_pattern空→既存ロジック |
| 旧メーカー一覧（配送パターンシートなし） | KeyError→delivery_patterns空→既存ロジック |
| 京葉/姫路（新ファイル使用） | "仙台" not in branch.name→即None→既存ロジック |
| 仙台+パターン未設定顧客 | get_customer_pattern→空文字→None→既存ロジック |

exeは1本で全営業所共通。新exeで旧顧客マスターを読んでも従来ロジックで正常動作する。

### 2. 顧客マスター_v2.xlsmの更新

D列（路線便）とE列（メールアドレス1）の間に「配送パターン」列を挿入。

**新構成:** A:顧客名 B:出荷曜日 C:保持日数 D:路線便 **E:配送パターン** F～:メールアドレス

- テーブル定義（テーブル2）をA1:T88に更新、列ID1～20で再構築
- ハイパーリンク付きセルの破損対策として、insert_colsではなく手動セルシフト方式で実装
- 全87行×全列のデータ一致を検証済み
- E列は全行空欄（京葉・姫路はこのまま使える。仙台の顧客に「近隣2便」「遠方午前」「遠方午後」を入れる）

### 3. メーカー一覧.xlsxに「配送パターン」シート追加

営業所設定シートと同じスタイル（濃紺背景＋白太字、テーブル形式）で新シートを作成。

| パターン名 | cutoff1 | cutoff1前 | cutoff2 | cutoff2前 |
|--|--|--|--|--|
| 近隣2便 | 11:30 | 当日 | 16:00 | 翌日 |
| 遠方午前 | 16:00 | 翌日 | | |
| 遠方午後 | 11:30 | 当日 | | |

コードはこのシートを列番号固定で読み取り、DeliveryPatternオブジェクトに変換する。

## 2/27の作業内容（家のPC）

### 1. git pull＆網羅的コード品質調査

家のPC（C:\repos\nouki_kaitou）でgit pull実施。9db3738→ad2c22cまでFast-forwardで21ファイル更新（仙台対応の一式）。

Claude Codeに6項目の網羅的コード品質調査を実施。全項目を並列Task実行。

#### テスト全件実行
1,077件全パス（7.90秒）。失敗なし。

#### import整合性チェック
- 存在しないimport: 問題なし
- 循環import: 問題なし
- 未使用import: 本体4件検出（data_loader.py:parse_time、excel_writer.py:OrderRow、stockout.py:ColumnMap、sendai_delivery.py:get_branch_settings）

#### 関数の重複・不整合
重複3件検出:
- **(A) get_rep_email_addresses（要修正）:** email_builder.py:80 と representative.py:143 に重複定義。「様」除去のlen>1ガードがemail_builder.py側のみ欠落
- (B) group_order_numbers_by_customer: 入力型が異なる2版あり（許容可能）
- (C) create_outlook_drafts / create_outlook_sends: 約140行の大部分が重複、末尾のSave() vs Send()のみ違い（リファクタリング推奨）

#### 仙台対応の結合確認
全チェックポイント正常。delivery_calc.py→sendai_delivery.pyの分岐挿入、cache.pyのbuild_pattern_cache、customer.pyのget_customer_patternすべて正しく結合。

#### エッジケース・防御コード確認
全体的に堅牢。実害のあるリスクなし。
- 低リスク: history.pyのソートキーがdatetime.datetimeのみチェック（datetime.date型はdatetime.min扱い）
- 極低リスク: cache.py:148でrow[0]がNoneのときstr(None)がキーに登録される（実運用上は発生しない）

#### CLAUDE.mdとの整合性
矛盾2件+記載漏れ1件:
- 15時カットオフの記述が「以前」→実装は「より前」（矛盾）、TODO「統一すべき」は既に統一済み
- ファイル構成にconfig.py、cache.py、sendai_delivery.pyが未記載

### 2. get_rep_email_addresses重複解消（5320966）

email_builder.pyの関数定義（約45行）を削除し、representative.pyからimportする1行に統一。representative.py側にはlen>1ガードがあり、「様」除去時の安全性が確保されている。既存テストは変更不要（re-importで動作）。

### 3. CLAUDE.md更新（5320966）

- 15時カットオフの記述修正: 「15:00以前」→「15:00より前（14:59以前）」、TODO→統一済みの旨に書き換え
- ファイル構成にconfig.py、cache.py、sendai_delivery.pyを追記

### 4. 未使用import 4件削除（5320966）

- data_loader.py: parse_time
- excel_writer.py: OrderRow
- stockout.py: ColumnMap
- sendai_delivery.py: get_branch_settings

### 5. 社外コメント表示機能の実装

#### B案（備考欄追記）→ A案（注番列置き換え）への方針変更

最初にClaude Codeがコード分析でB案（備考欄に社外コメントを追記）を推奨。実装してテストも通ったが、**備考欄は全角13文字が上限で社外コメントを追記するには狭すぎる**ことと、藤井課長が「幅に制約あれば、マツモト注番不要」と明言していたことから、A案（注番列を社外コメントに置き換え）に変更。

#### A案の設計

- 営業所設定シートG列「注番列の表示」に値を入力:
  - 空欄 → L列ヘッダー「弊社注番」、全行に注番（従来通り）
  - `連絡事項` → L列ヘッダー「連絡事項」、社外コメント表示
- 社外コメントから送り状番号・引取テキストを自動除去（clean_external_comment関数）
- 社外コメントが空（送り状番号のみ等）の場合はL列も空欄（注番にフォールバックしない）
- 備考欄（K列）は一切変更なし

#### 変更ファイル

| ファイル | 変更内容 |
|--|--|
| models.py | BranchSettings に remarks_mode: str = "detail" 追加 |
| config.py | max_col=6→7、G列が「連絡事項」なら remarks_mode = "external" |
| tracking.py | clean_external_comment() 関数追加（送り状・引取テキスト除去） |
| excel_writer.py | create_headerにbranch引数追加（L列ヘッダー切替）、copy_data_rowにexternal_comment引数追加（L列の値切替） |
| report_generator.py | create_headerにbranchを渡す、copy_data_rowでremarks_mode=external時にクリーニング済み社外コメントを渡す |

#### 社内コメント漏洩の安全性確認

comment_internalがプロダクションコードで参照される全8箇所を確認。全箇所でマーカー有無のbool判定・マーカー直後の日付抽出・制御フラグ設定のみに使用。「／．８５」等の仕入れ価格情報が回答書に漏れるパスは存在しない。

### 6. 送り状番号除去パターンの修正（5caa4d2）

「佐川急便１９８６－５５５７－６３４６」が除去されずにL列に表示されるバグを修正。

**原因:** _TRACKING_PATTERNのキーワードリストに「佐川」はあったが「佐川急便」がなく、「佐川急便１９８６...」だと「佐川」の後の「急便」がどのパターンにもマッチしなかった。

**修正:** _CARRIER_KEYWORDS（略称）と_CARRIER_FULL_NAMES（正式名称）の和集合を長さ降順でソートし、最長一致で正規表現を構築。全角数字・全角ハイフンにも対応。

### 7. 回帰検証（仙台対応+社外コメント表示）

全12項目を検証し、既存営業所への影響ゼロを確認:
- remarks_mode未設定時のL列: 従来通り注番表示
- 仙台以外でsendai_delivery.py不発動: 「仙台」not in branch.nameで即None
- build_customer_cacheの4-tuple化: 旧フォーマットでも安全（ヘッダベース検出+ダブルガード）
- create_headerのbranch引数: デフォルトNoneで既存呼び出し全て安全
- copy_data_rowのexternal_comment引数: デフォルトNoneで既存呼び出し全て安全
- config.pyのmax_col=7拡張: len(row)>6ガードで旧メーカー一覧も安全
- B案ロジックの完全revert: show_external_commentのgrep結果0件
- tracking.pyの既存関数: 変更なし、_CARRIER_KEYWORDS参照も壊れていない

### 8. メーカー一覧.xlsxの加工

営業所設定シートにG列「注番列の表示」を追加。松本営業所の行に「連絡事項」を入力。ヘッダースタイル（濃紺背景+白太字）と罫線をA〜F列に合わせて設定。

### 9. 藤井課長への返信メール作成

ツール一式の差し替え依頼、社外コメント対応の説明（L列の切替仕様、G列の設定方法、注番に戻す方法）、仙台対応に伴うメーカー一覧・顧客マスターの構成変更について。

## 今後の改善アイデア（2/22検討）
- 処理結果サマリー表示（確定○件、確認中○件の全体像を実行後に表示）
- 確認中アラート（7日以上未回答の件数をexe実行時に警告）
- 出荷予定日が近づいたらリマインドメール（顧客向け）
- 納期変更時の差分通知（顧客向け）
- 回答書メール本文に要注意案件のハイライト（欠品中○件、分納○件）
- 回答書に未出荷案件を自動追加（前回回答済みで変更なしの案件も掲載）

## 検証の全体像

| 検証方法 | 件数 | 発見バグ |
|--|--|--|
| テスト | 1,109件 | 0（仕様漏れは見つけられない） |
| データ比較 | 1,521件 | 0（メール本文は比較対象外） |
| コード突合せ（2/18） | 84関数+15関数の分岐 | 2件（両方修正済み） |
| コード突合せ（2/21） | 83関数再突合せ | 3件（全て修正済み） |
| 影響確認（2/22） | 971テスト+等価性検証 | 0件（改善方向の差異1件のみ） |
| 分納・欠品ライフサイクル検証（2/22） | 全パターン洗い出し+18テスト追加 | 0件（ロジックに問題なし） |
| 業務ルール確認表 vs コード突合せ（2/23） | 45ルール+73具体例+28用語 | 2件（正規化漏れ+出荷判定漏れ、両方修正済み） |
| 全分岐比較（2/23） | 3関数×8分岐パターン | 0件（差分3件は全て意図的） |
| 路線便網羅検証（2/25） | 全6パス+days_to_add-1の5箇所+テスト3件追加 | 0件（全パスで「出荷予定」確認済み） |
| 仙台配送パターン対応（2/26） | 新規87テスト+既存990件の回帰テスト | 0件（1,077件全パス） |
| 網羅的コード品質調査（2/27） | 6項目並列検証（import/重複/結合/エッジケース/CLAUDE.md） | 重複1件修正、CLAUDE.md矛盾2件修正、未使用import4件削除 |
| 回帰検証（2/27） | 全12項目（仙台対応+社外コメント表示の既存影響確認） | 0件（全項目問題なし） |
| 社内コメント漏洩確認（2/27） | comment_internal参照全8箇所の出力パス確認 | 0件（テキスト自体が出力されるパスなし） |
| コード品質調査（3/1、スキル入り5エージェント並列） | 6項目×5エージェント | H3クローズ漏れ修正、H2到達不能コード整理。L1/L2は誤判定 |
| 過去の削除の安全性確認（3/1） | import4件+関数1件 | 0件（全て正しく削除されていた） |
| 分納パターン個別テスト（3/1） | 7パターン | 0件（H2修正で動作変化なし） |
| 回帰テスト（3/1） | 1109件全パス | 0件 |

**3方向から検証して、見つかったバグは全て修正済み。**

## 未突合せ（低リスク）
- CreateHeader / FormatReport（見た目の問題のみ）
- ユーティリティ系（単純な変換関数）

## 留意事項
- ~~get_rep_email_addressesがemail_builder.pyとrepresentative.pyに重複定義あり~~ → **2/27修正済み。representative.pyに一本化**
- compare_realtime.pyによる日次比較は継続すべき
- テザリング環境ではネットワーク上のexe起動に時間がかかる（社内LANでは問題ない見込み）
- GitHubリポジトリ（kento1984/nouki_kaitou）をManus用にパブリックにした → 作業後プライベートに戻すこと
- 家のPCのリポジトリ: C:\repos\nouki_kaitou、再ビルドコマンド: `cd C:\repos\nouki_kaitou && python -m PyInstaller nouki_kaitou.spec --clean --noconfirm`
- 会社のPCのリポジトリ: C:\Users\kento.kashiwabara\nouki_kaitou、再ビルドコマンド: `cd C:\Users\kento.kashiwabara\nouki_kaitou && pyinstaller nouki_kaitou.spec --clean --noconfirm`
- 会社PCのCドライブ空き: 約3GB（2/26時点。Downloads 3.3GBと.cursor 567MB削除で確保）。Claudeデスクトップアプリは入れない。週1で掃除バッチ実行推奨
- exe名は「納期回答書作成.exe」に変更済み（2/24）。nouki_kaitou.specのnameを変更
- **openpyxlのロック問題:** ネットワーク上の顧客マスター_v2.xlsmを誰かがExcelで開いていると、openpyxlで読み込んだセルの値がNoneになる。「該当するデータがありません」が出たら、顧客マスターを閉じてもらうことで解決（2/24発見）
- **parse_date()の対応形式（2/25拡張済み）:** YYYY/M/D, YYYY-M-D, M/D（年補完）, Excelシリアル値（整数/float）, datetimeオブジェクト。確認中一覧のJ列に `3/1` と入力してもExcelが46082に変換しても、どちらでも正しく認識される。
- 納期回答書作成.exeはファイルサーバー `\\flsv04\316京葉\納期回答書ツールフォルダ` にコピー済み（2/24）
- **全社共有フォルダ:** `\\flsv04\001全社_共有\納期回答書作成ツール` にも配置。松本営業所等はこちらから取得。
- **顧客マスターの新旧判定:** ヘッダ行（1行目）のE列が「配送パターン」であれば新フォーマット、それ以外（「メールアドレス1」等）であれば旧フォーマットと自動判定。旧フォーマットでも新exeは正常動作する。（当初は値ベースの@判定だったが、read_only=Falseでmailto:が漏れ出す問題があり、ヘッダベースに変更済み）
- **仙台対応のexe配布戦略:** exeは1本で全営業所共通。仙台には仙台対応入りの新exeをすぐ配布。京葉・姫路は今のexeを使い続け、次の更新タイミングで自然に切り替わる。
- **低優先度リファクタリング（2/27調査で検出）:** ①history.pyのソートキーでdatetime.date型にも対応 ②create_outlook_drafts/create_outlook_sendsの重複約140行をヘルパー抽出 ③group_order_numbers_by_customerのリネームで区別明確化。いずれも実害なし、次の機会でOK。
- **スキルファイルはGitにコミット済み（3/1）。** 会社PCでgit pullすれば `.agents/skills/` が同期され、会社PCのClaude Codeでもスキルが効く
- **AIの調査結果は間違えることがある（3/1発覚）。** L1/L2「未使用関数」の誤判定があった。Claude Codeの出力を賢人さんが疑って再確認するワークフローは維持すること
- **M1〜M4, M6〜M8, L3〜L5は未対応（3/1調査で検出）。** 実害なしのリファクタ候補。次回機能追加のついでに対応で十分
- **スキルの入れすぎ注意:** スキルはコンテキストウィンドウを消費する。プロジェクトごとに3〜5個が目安。不要なスキルは `.agents/skills/` から該当フォルダを削除

## 未実装・保留
- **月曜のタスク（2/27作業の残り）:** ①会社PCでgit pull ②exe再ビルド ③メーカー一覧.xlsx（G列追加済み）をファイルサーバーに配置 ④exeをファイルサーバーに配置 ⑤藤井課長にメール送信
- **仙台営業所の運用開始準備:** コード実装・デバッグ完了。残りはデータ準備のみ。①佐藤さんから顧客ごとの配送パターン分類（近隣2便/遠方午前/遠方午後）をもらう ②顧客マスターE列にパターン名を入力 ③営業所設定シートに仙台の行を追加 ④ファイルサーバーにexeコピー
- **会社PCでのビルド・デモフォルダテストは完了（2/26）。** ヘッダベース判定修正・当日配達の過去日修正を含むexeをデモフォルダに配置済み。近隣2便の実データでの動作確認が残っている（16時以降の伝票で遠方午前との差が確認できる）。ファイルサーバーへの本番配布は動作確認後。
- ~~社外コメントの表示機能~~ → **2/27実装完了。** 営業所設定シートG列「注番列の表示」に「連絡事項」と入力すると、L列が弊社注番→連絡事項（社外コメント、送り状・引取除去済み）に切替。松本営業所のみ設定済み。メーカー一覧.xlsxも加工済み。月曜に会社PCでpull→再ビルド→ファイルサーバー配布→藤井課長にメール送信で完了。
- ~~Level 1-C 処理結果サマリー（通常処理○件、手入力○件の表示）~~ → **3/1に基本版（確定/確認中/スキップ/合計）を実装済み。** 通常処理○件・手入力○件の内訳表示は今後実装予定
- 以下は実害なしのリファクタ候補（3/1調査で検出。急ぎではない）:
  - M1: 顧客名比較のヘルパー統一（report_generator.py, sendai_delivery.py）
  - M2: 180日マジックナンバーの定数化（3ファイル）
  - M3: 「転送中（直送用）」文字列の定数化（8箇所以上）
  - M4: _result()関数の重複解消（delivery_calc.py / sendai_delivery.py）
  - M6: 2/29年またぎの返却値統一（発生確率ほぼゼロ）
  - M7: 仙台+社外コメントの結合テスト追加（分離設計で干渉しない）
  - M8: 路線便use_ship_rule上書きタイミングの確認（2/23で意図的差異として確認済みの可能性）
  - L3: ステータス文字列のEnum化
  - L4: unit_price float 1.0の境界値確認
  - L5: CLAUDE.mdの「キャンセル」マーカーとコードの対応確認
- マニュアルv7の完成待ち（Manusに修正指示書を送付済み）
- 実画像への差し替え（NANOバナナ仮画像→実スクリーンショット）
- GitHubリポジトリをプライベートに戻す
- Claude Code/Cursorのキャッシュ先をDドライブに移動（Cドライブ容量対策）
- 掃除バッチファイルの修正（Shift-JIS保存が必要。文字化けエラーで未完了）
- 分納・欠品フロースライドの修正（欠品のステップ3・4を分納と同じ表現に揃える）

## 成果物一覧

### 2/21作成
| ファイル | 内容 |
|--|--|
| manus_instructions_final.md | Manus向けスライド作成指示書（業務知識セクション付き） |
| v4_revision_instructions.md | Manus v4への修正指示（スライド単位の修正＋デザイン修正） |
| 納期回答書作成ツール_運用マニュアル_v5.pdf | Manus作成のv5版（22枚、ほぼ完成） |

### 2/22作成
| ファイル | 内容 |
|--|--|
| 欠品の運用フロー.html | 欠品の入力→表示→解消→確認中一覧対応の業務担当者向け解説 |
| 分納の運用フロー.html | 分納の入力→表示→状態変化→確認中一覧対応の業務担当者向け解説 |

### 2/23作成
| ファイル | 内容 |
|--|--|
| manus_v7_revision_instructions.md | Manus向けv7修正指示書（7カテゴリ13箇所の修正＋スライド順番変更＋画面分割） |
| cc_verify_business_rules.md | Claude Code向け業務ルール確認表突合せ指示書 |
| cc_fix_two_bugs.md | Claude Code向けコードバグ2件修正指示書（全分岐比較含む） |
| cc_fix_business_rules_excel.md | Claude Code向け業務ルール確認表修正指示書（14件） |
| 業務ルール確認表.xlsx | 突合せ結果に基づき14件修正した確認表（再生成） |

### 2/26作成
| ファイル | 内容 |
|--|--|
| 顧客マスター_v2.xlsm | E列に「配送パターン」列を追加した新フォーマット |
| メーカー一覧.xlsx | 「配送パターン」シートを追加（近隣2便/遠方午前/遠方午後の定義） |

### 2/27作成
| ファイル | 内容 |
|--|--|
| メーカー一覧.xlsx | 営業所設定シートG列「注番列の表示」追加、松本営業所に「連絡事項」設定済み |
| 藤井課長向け返信メール | ツール一式差し替え依頼、社外コメント対応説明、仙台対応に伴う構成変更説明 |

## Gitコミット

### 2/18
- b1c65b3: 業務ルール確認表を作成
- bf3888d: bunno.pyの_QTY_UNITSに「ケース」追加
- 8f60861: StockoutEntryにdeliveryフィールド追加、欠品確定納期をメールに反映

### 2/19
- 324ba87: PyInstaller exe化対応: エントリポイントrun_gui.py追加
- 3c83c21: exe起動時にコンソールへ案内メッセージを表示
- 6f678e7: 在庫販売+処理完了: 土日祝の登録日を翌営業日に補正してから納期計算

### 2/21
- （コミットハッシュはClaude Code側で確認）: 担当者マスター接続追加
- （コミットハッシュはClaude Code側で確認）: 180日超履歴自動削除実装
- （コミットハッシュはClaude Code側で確認）: ファイルロックチェック実装
- a4ec395: データファイル除外（.gitignore追加、Excelファイル等をGit追跡から除外）

### 2/22
- a8bbfca: GUI→run間のマスター重複読み込み解消（約1秒短縮）
- （送付履歴バッチ書き込みのコミットハッシュはClaude Code側で確認）: load_workbook(read_only=False)廃止で送付履歴保存20秒→2.67秒
- 42e7e56: 確認中一覧: 一部処理済みに薄緑の色分け追加
- 606235f: 送り状: 数量表示の正規化（1.00→1, 2.50→2.5）
- （一部処理済みの文字列修正のコミットハッシュはClaude Code側で確認）: 「一部処理済」「一部処理済み」両方に対応
- （分納・欠品ライフサイクルテストのコミットハッシュはClaude Code側で確認）: 分納・欠品ライフサイクル統合テスト18件追加（テスト計971件）

### 2/23
- （コミットハッシュはClaude Code側で確認）: _resolve_delivery_placeの正規化漏れ修正 + _calc_normalに紐付き出荷判定追加（テスト計975件）
- （コミットハッシュはClaude Code側で確認）: 業務ルール確認表をコード突合せ結果に基づき14件修正

### 2/24
- d0e81ad: 小画面対応: ウィンドウ高さを画面解像度に合わせて動的調整
- 4a673ed: exe名を納期回答書作成に変更（nouki_kaitou.specのname変更）
- c50dee9: 数量列の小数表示修正: 12.5が13に四捨五入表示される問題を修正
- ededfc0: 送り状キーワードに福通・近物・新潟・名鉄・エクスプレスを追加

### 2/25
- 2dec631: parse_dateで月/日のみ入力（M/D形式）に対応（年補完+過去なら翌年）
- 05ef812: parse_dateでExcelシリアル値（整数/float）に対応
- 6f32d60: 路線便（is_rosenbin）の不足テスト3件追加（テスト計990件）

### 2/26
- 0ebb03c: 仙台営業所の配送パターン対応（sendai_delivery.py新規、models/cache/customer/delivery_calc/main/email_builder変更、テスト61件追加）
- （紐付き+処理完了対応は0ebb03cに含まれる）: check_sendai_himozuki_completed追加、テスト19件追加
- （当日配達サフィックスは0ebb03cに含まれる）: 「配達済み」→「配達予定」に修正（テスト期待値6箇所修正、テスト計1,071件）
- 0a78dfc: _detect_customer_master_formatをヘッダベース判定に変更（read_only=Falseでmailto:が漏れ出す問題を修正、テスト計1,073件）
- ad2c22c: 当日配達(biz_days==0)の過去日を配達済みに修正（テスト計1,077件）

### 2/27
- 5320966: リファクタリング: get_rep_email_addresses重複解消（representative.pyに一本化）、CLAUDE.md更新（カットオフ記述修正+ファイル構成追記）、未使用import4件削除
- （社外コメント表示機能のコミットハッシュはClaude Code側で確認）: 社外コメント表示機能実装（B案→A案変更、remarks_mode=externalでL列を連絡事項に切替）
- （営業所設定G列日本語化のコミットハッシュはClaude Code側で確認）: 営業所設定G列を日本語化（external→連絡事項）、configテスト4件追加
- ce3fd91: remarks_mode=external時のフォールバック廃止（社外コメント空→L列も空欄に）
- 5caa4d2: clean_external_comment: 正式名称+全角数字+ハイフン区切りの送り状番号除去に対応（テスト計1,109件）

### 3/1
- 67bfc45: 処理結果サマリー表示を追加（確定/確認中/スキップ/合計）+ obra/superpowersスキルインストール
- d20261f: H3/M5: ワークブックのクローズ漏れ修正（try-finally化）
- 8b13b4a: H2: _classify_orderのhas_bunno_mitei重複呼び出し・到達不能コード整理

## 3/1の作業内容（家のPC）

### 1. skills.shからClaude Code用スキルをインストール

skills.sh（https://skills.sh/）からAIエージェント用のスキルをClaude Codeにインストール。スキルの正体は `.agents/skills/` に置かれるMarkdownファイルで、Claude Codeが作業開始時に自動で読み込む（CLAUDE.mdと同じ仕組み）。

**インストール先:** C:\repos\nouki_kaitou

**インストールコマンド:** `npx skills add obra/superpowers --yes`
（個別指定がエラーになったため、obra/superpowers全体を一括インストール）

**インストールされたスキル:**
- systematic-debugging — 体系的なデバッグ手法
- test-driven-development — テスト駆動開発
- verification-before-completion — 完了前の検証プロセス
- brainstorming — ブレスト支援
- executing-plans / writing-plans — 計画の作成と実行
- subagent-driven-development — サブエージェントを活用した開発
- using-git-worktrees — Git worktreeの活用
- dispatching-parallel-agents — 並列エージェントの活用
- その他

**スキルはClaude Code専用ではない。** Cursor、Cline、GitHub Copilot、Windsurf等18種類のエージェントに対応。

**スキルの効果（観察結果）:**
- subagent-driven-development → 効いた。コード品質調査で5つのExploreエージェントを並列起動
- systematic-debugging → 効いた。調査レポートの粒度が前回（2/27）より細かくなった
- test-driven-development → 効いてない。テストを先に書かずにいきなり実装に入った
- verification-before-completion → 微妙。pytest全件実行はしたが、指示に書いたからかスキルの効果かは不明

**注意:** スキルが入ってもClaude Codeの調査結果は間違えることがある（後述L1/L2誤判定）。最後に賢人さんが確認するワークフローは変えない。

**コミット:** 67bfc45（スキルファイルも一緒にコミット。会社PCでpullすればスキルが効く）

### 2. 処理結果サマリー表示機能（67bfc45）

exe実行後に処理結果の全体像をコンソールに表示する機能を追加。

**出力イメージ:**
```
========================================
  処理結果サマリー
========================================
  確定:      12件
  確認中:    3件
  スキップ:  5件
  合計:      20件
========================================
```

**変更ファイル:** main.py（4箇所）
1. `skipped_order_count = 0` 初期化（L639）
2. `--customer`指定時のスキップ伝票数計上（L668）
3. 全顧客モード時のスキップ伝票数計上（L676）
4. サマリー表示（L819-829）

**カウントの意味:**
- 確定 = `len(all_confirmed)` — 送付履歴に移動した伝票数
- 確認中 = `len(all_confirming)` — 確認中一覧に残った伝票数
- スキップ = 顧客マスター未登録で除外された伝票数

テスト全1109件パス。

### 3. コード品質の網羅的調査（スキル入り）

スキルインストール後の初回調査。5つのExploreエージェントを並列起動して6項目を調査。

**調査結果:**

| 深刻度 | 件数 | 内容 |
|--|--|--|
| 高 | 3件 | H1:紐付き判定の条件差異、H2:has_bunno_mitei到達不能コード、H3:history_wb_roクローズ漏れ |
| 中 | 8件 | M1-M8:定数散在、重複関数、テスト漏れ等 |
| 低 | 5件 | L1-L5:未使用関数、Enum化の余地等 |
| 問題なし | 14項目 | テスト全パス、CLAUDE.md整合、仙台・社外コメント結合 |

**H1は修正不要（意図的な差異）。** report_generator.pyのis_himozukiは「紐付きかどうか」の判定（force_deliveredブロック用）、delivery_calc.pyのis_himozuki_diffは「紐付き＋受注先≠出荷先」の判定（配達/出荷切替用）。目的が違うから条件が違う。2/23の全分岐比較で確認済み。

**L1/L2「未使用関数」は誤判定だった。** 
- L1: extract_date_from_string() → excel_writer.pyから4箇所で呼ばれている（現役）
- L2: group_order_numbers_by_customer() → テスト3ファイルからimportされている（削除するとテスト壊れる）

**教訓:** AIの調査結果も間違える。「本当に使われてないの？」と疑って再確認することが重要。

### 4. H3/M5: ワークブックのクローズ漏れ修正（d20261f）

main.pyのrun()関数で、ワークブックを開いた後に例外が発生した場合にファイルハンドルがリークする問題を修正。

**H3: history_wb_ro（送付履歴）**
- キャッシュ構築〜行データ抽出（L588-608）をtryで囲み、finallyでhistory_wb_ro.close()を確実に実行
- 旧 `if history_exists:` 条件を撤廃（新規作成時のワークブックもクローズ対象に）

**M5: mfg_wb / cust_wb（メーカー一覧・顧客マスター）**
- run()の本体（L559〜L834）を外側のtryで囲み、finallyでmfg_wb.close() / cust_wb.close()を確実に実行

**構造:**
```
run():
    ... ワークブック読み込み ...
    try:                              ← M5: 外側try
        ... 送付履歴オープン ...
        try:                          ← H3: 内側try
            ... キャッシュ構築・履歴読込 ...
        finally:
            history_wb_ro.close()     ← H3: 確実にクローズ
        ... 回答書生成・保存・メール ...
    finally:
        mfg_wb.close()                ← M5: 確実にクローズ
        cust_wb.close()
```

テスト全1109件パス。267行の差分だが、大半はインデント変更でロジック変更なし。

### 5. H2: _classify_orderの到達不能コード整理（8b13b4a）

report_generator.py の `_classify_order()` 内で `has_bunno_mitei()` を2回呼んでいたが、2回目は到達不能コードだった問題を修正。

**修正前:**
```python
if has_bunno_mitei(...):       # 1回目：Trueなら「分納」
    ship_status = "分納"
elif prev_status == "分納":
    if has_bunno_mitei(...):   # 2回目：1回目がFalseだから常にFalse → 到達不能
        ship_status = "分納"
    else:
        # 分納完了
else:
    # 分納完了
```

**修正後:**
```python
if has_bunno_mitei(...):       # 1回だけ呼ぶ
    ship_status = "分納"
else:
    # 未定なし（全分確定 or 新規で未定なし）→ 分納完了
```

17行 → 1行に削減。CLAUDE.mdの既知の課題からも該当項目を削除。テスト全1109件パス。

**分納パターン全7件のテスト個別確認済み:**
- 分納+未定あり → 確認中「分納」 ✓
- 分納+全確定（新規）→ 送付履歴「分納完了」 ✓
- 分納+全確定（既存確認中）→ 送付履歴「分納完了」 ✓
- 既存分納+未定あり → 確認中に留まる ✓
- 分納+処理完了 → 送付履歴「分納完了」 ✓
- 分納完了通知の収集 ✓
- 分納ライフサイクル ✓

### 6. 過去の削除の安全性確認

L1/L2の誤判定を受けて、2/27に削除したコードが問題を起こしていないか確認。

**2/27に削除した未使用import 4件:**
| 削除対象 | 現状 | 判定 |
|--|--|--|
| data_loader.py: parse_time | data_loader.py内に参照なし | 問題なし |
| excel_writer.py: OrderRow | excel_writer.py内に参照なし | 問題なし |
| stockout.py: ColumnMap | stockout.py内に参照なし | 問題なし |
| sendai_delivery.py: get_branch_settings | sendai_delivery.pyからの参照なし | 問題なし |

**2/27に削除したget_rep_email_addresses (email_builder.py):**
- representative.pyに一本化済み
- email_builder.py:27で正しくrepresentative.py版をimport
- テストも全てrepresentative.py版を参照
- 問題なし

pytest全1109件パスで実証済み。

### 7. 回帰テスト（今日の全変更）

全1109テストパス。分納関連テスト7件を個別に確認し、H2修正によるロジック変更がないことを検証。H3/M5のtry-finally化はインデント変更のみで変数スコープに影響なし（Pythonはブロックスコープではない）。


